# Hugo configuration file
title: Schmidt DSE Fire Severity Tool (Version 0)

# import hextra as module
module:
  imports:
    - path: github.com/imfing/hextra

markup:
  # allow raw html
  goldmark:
    renderer:
      unsafe: true

  # enable hextra syntax highlight
  highlight:
    noClasses: false

menu:
  main:
    - name: Home
      pageRef: /
      weight: 1
    - name: Upload
      pageRef: /upload
      weight: 2
    - name: Directory
      pageRef: /directory
      weight: 3
    - name: Demo Map
      pageRef: /demo_map
      weight: 4
    - name: ⠀⠀⠀⠀
      pageRef:
      weight: 5
    - name: About
      pageRef: /about
      weight: 6
    - name: People
      pageRef: /people
      weight: 7
    - name: DSE ↗
      url: "https://dse.berkeley.edu/"
      weight: 8
    - name: GitHub
      weight: 9
      url: "https://github.com/SchmidtDSE"
      params:
        icon: github

params:
  navbar:
    displayTitle: true
    displayLogo: false

  footer:
    enable: false
